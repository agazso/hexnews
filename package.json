{
  "name": "hexnews",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "indexer": "npx ts-node indexer",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ts-node cmd build && npx esbuild app.ts --bundle --outfile=dist/hexnews.js --global-name=hexnews"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.15.8",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "@ethersphere/bee-js": "file:../../bee-js"
  }
}
