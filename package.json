{
  "name": "hexnews",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ts-node build && npx esbuild app.ts --bundle --outfile=dist/hexnews.js --global-name=hexnews"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.15.8",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "@ethersphere/bee-js": "^5.0.0"
  }
}
