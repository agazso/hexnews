{
  "name": "hexnews",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "indexer": "npx ts-node src/indexer",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ts-node src/cmd build && npx esbuild src/app.ts --bundle --outfile=dist/hexnews.js --global-name=hexnews"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.15.8",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "@ethersphere/bee-js": "file:../../bee-js"
  }
}
